package app

import (
	"testing"
)

func TestInstallTpm(t *testing.T) {
	// InstallTpm()

	// expectedDirPath := filepath.Join(os.Getenv("HOME"), ".tmux", "plugins", "tpm")
	// if _, err := os.Stat(expectedDirPath); errors.Is(err, os.ErrNotExist) {
	// 	t.Fatalf("Directory does not found to %s", expectedDirPath)
	// }

}
